# creative-gallery-astro

Minimal Astro scaffold created by setup script.

Getting started

1. Install dependencies:

```powershell
npm install
```

2. Start dev server:

```powershell
npm run dev
```

3. Build for production:

```powershell
npm run build
```

Notes
- This repository contains a minimal Astro scaffold. If you prefer the official starter templates, you can run `npm create astro@latest .` instead.
