import { defineConfig } from 'astro/config';

export default defineConfig({
  // No `site` set for local development. Add a full URL (e.g. "https://example.com")
  // when deploying so canonical URLs are generated correctly.
});
